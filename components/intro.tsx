import { CMS_NAME } from '../lib/constants'

const Intro = () => {
  return (
    <section className="flex-col md:flex-row flex items-center md:justify-between mt-2 mb-2">
      <h1 className="text-2xl md:text-2xl md:pr-8">
        Bṛhat Open Library
      </h1>
    </section>
  )
}

export default Intro
